[pytest]
testpaths = tests
markers =
    smoke: plays whole worlds (the 300-season sanity run); minutes, not seconds.
        Excluded from the engine workflow with -m "not smoke" — it tests the
        rules rather than any one run, and belongs in a Code session.
